# validator-devops

```
$ curl -H "Content-Type: application/json" -d'{"id":1, "jsonrpc":"2.0", "method": "author_rotateKeys", "params":[]}' http://localhost:9933
request native token of appchain
set session key on octopus website
$ kubectl rollout restart sts *-deployment
```
